.386
STACK	SEGMENT	USE16	STACK
	DB 200 DUP(0)
STACK	ENDS
CODE SEGMENT USE16
ASSUME CS:CODE,SS:STACK

START:
	XOR AX,AX
	XOR BX,BX
	XOR CX,CX
	MOV DS,AX
	MOV BX,DS:[58H]
	MOV CX,DS:[5AH]
	MOV AH,4CH
	INT 21H
	
CODE ENDS
END	START
	