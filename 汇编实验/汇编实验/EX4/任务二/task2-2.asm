.386
DATA SEGMENT USE16
DATA ENDS
CODE SEGMENT USE16
	ASSUME CS:CODE,SS:STACK

START:
	XOR AX,AX
	MOV DS,AX
	MOV AX,3516H
	INT 21H
	MOV DI,ES:[BX-2]
	MOV SI,ES:[BX-4]
	CLI
	MOV DS:[16H*4+2],DI
	MOV DS:[16H*4],SI
	STI
	MOV AH,4CH
	INT 21H
	
CODE ENDS
STACK SEGMENT USE16	STACK
	DB 200 DUP(0)
STACK ENDS
END	START